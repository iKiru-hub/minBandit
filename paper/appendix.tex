\section{Appendix}\label{sec:appendix}

% ---------------------------------

\subsection{Neural response function}

The activation functions applied to the two neuronal population are defined as a step-function composed with a generalized sigmoid as follows:

\begin{equation}
f(x; g, o, \theta) =
\begin{cases}
    \left[1 + e^{-g(x - o)}\right]^{-1} & \text{ if } \left[1 + e^{-g(x - o)}\right]^{-1} > \theta \\
0 & \text{ otherwise}
\end{cases}
\end{equation}

Where:
\begin{itemize}
\item $x$ is the neuron pre-activation value
\item $g$ is the gain
\item $o$ is the offset
\item $\theta$ is the threshold
\end{itemize}
\noindent Each population has its own set of parameters, which are optimized through evolutionary search.

% ---------------------------------

\subsection{Gaussian-sigmoid function}\label{func:gs}

\noindent The function $\Phi_{\cdot}$ is defined by combining a generalized version of the sigmoid, namely with a gain $\beta \neq 1$ and offset $\alpha\neq 0$, and a Gaussian with mean $\mu$ and variance $\sigma^{2}$. Their contributions are weighted by as $r$ and $1-r$ ($r\in(0,1)$) respectively.

\begin{equation*}
    \Phi_v(x) = r\left(1 + \exp^{-\beta(x-\alpha)}\right)^{-1} + (1-r)\exp\left(-\frac{(x-\mu)^2}{2\sigma^2}\right)
\end{equation*}

\noindent The motivation behind this choice is to express a function that possesses a bounded region (depending on $\mu,\,\sigma$) at a high/low peak (depeding on the value of $\gamma_{2}$), and a continuous transition to a constant value (depending on the steepness of the sigmoid $\beta$, shift
$\alpha$, and intensity $\gamma_{1}$).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figure_appx_1.png}
    \caption{\textsc{Activation function $\Phi_{v}$} - \textit{Parameters $\beta=10$, $\alpha=1$, $\mu=1$, $\sigma=1$, and $r=0.5$.}}
    \label{fig:gau_sigm}
\end{figure}

% ---------------------------------

\subsection{Baseline literature models}

The algorithms we chose to compare our model with are well-known in the literature of machine learning and reinforcement learning, although not representing the current state-of-the-art.
They were implemented in native Python, and only $\epsilon-$Greedy required hyper-parameters calibration.

\subsubsection{Upper Confidence Bound (UCB1)}

\begin{algorithm}[H]
\SetAlgoLined
\caption{Upper Confidence Bound algorithm}
\SetKwComment{Comment}{// }{ }
\KwIn{Number of arms $K$}
\KwOut{Sequence of selected arms $A_t$ and observed rewards $R_t$}
\textbf{Initialize:}\;
\quad Total time steps $t \gets 0$\;
\quad For each arm $k \in \{1, \ldots, K\}$: initialize count $n_k \gets 0$ and average reward estimates $Q_k \gets 0$\;

\textbf{Exploration:}\;
\quad \For{$k \gets 1$ \KwTo $K$}{
    Select arm $k$, observe reward $r_k$\;
    $n_k \gets 1$\;
    $Q_k \gets r_k$\;
    $t \gets t + 1$\;
}

\textbf{Main:}\\
\While{True}{
    $t \gets t + 1$\;

    \tcc{Calculate the UCB index for all arms}
    \For{$k \gets 1$ \KwTo $K$}{
        $B_k \gets \sqrt{\frac{2 \ln t}{n_k}}$ \Comment*[r]{Confidence bonus (exploration)}
        $\text{UCB}_k \gets Q_k + B_k$ \Comment*[r]{Average reward (exploitation) and Bonus}
    }

    \tcc{Select the arm with the highest UCB index}
    $k^* \gets \arg\max_{k \in \{1, \ldots, K\}} \text{UCB}_k$\;

    \tcc{Observe the reward and update statistics}
    Select arm $k^*$, observe reward $r_{k^*}$\;
    $n_{k^*} \gets n_{k^*} + 1$\;
    $Q_{k^*} \gets Q_{k^*} + \frac{1}{n_{k^*}} (r_{k^*} - Q_{k^*})$ \Comment*[r]{Incremental mean update}
}
\label{alg:ucb1}
\end{algorithm}

% --
\subsubsection{Thompson Sampling (TS)}

\begin{algorithm}[H]
\SetAlgoLined
\caption{Thompson Sampling algorithm}
\SetKwComment{Comment}{// }{ }
\KwIn{Number of arms $K$}
\KwOut{Sequence of selected arms $A_t$ and observed rewards $R_t$}

\textbf{Initialize:}\\
\quad For each arm $k \in \{1, \ldots, K\}$:\;
\quad \quad Initialize success count prior $\alpha_k \gets 1$\;
\quad \quad Initialize failure count prior $\beta_k \gets 1$\;

\textbf{Main:}\\
\While{True}{
    \tcc{Sampling from the posterior distribution}
    \For{$k \gets 1$ \KwTo $K$}{
        %Sample expected reward $\theta_k \sim \text{Beta}(\alpha_k, \beta_k)$\;
        $\theta_k \gets \sim \text{Beta}(\alpha_k, \beta_k)$ \Comment*[r]{sample expected reward}
    }

    $k^* \gets \arg\max_{k \in \{1, \ldots, K\}} \theta_k$ \Comment*[r]{Select the arm with the highest sampled value}

    \tcc{Observe the reward (must be $r_{k^*} \in \{0, 1\}$) and update the posterior}
    Select arm $k^*$, observe binary reward $r_{k^*}$\;
    $\alpha_{k^*} \gets \alpha_{k^*} + r_{k^*}$ \Comment*[r]{updated success count}
    $\beta_{k^*} \gets \beta_{k^*} + (1 - r_{k^*})$ \Comment*[r]{updated failure count}
}
\label{alg:thompson}
\end{algorithm}


% --
\subsubsection{Epsilon-Greedy (\texorpdfstring{$\epsilon$-Greedy}{epsilon-Greedy})}\label{alg:epsg}

\begin{algorithm}[ht]
\caption{$\epsilon$-Greedy algorithm}
\SetAlgoLined
\SetKwComment{Comment}{// }{ }
\KwIn{Number of arms $K$, exploration parameter $\epsilon \in [0, 1]$}
\KwOut{Sequence of selected arms $A_t$ and observed rewards $R_t$}

\textbf{Initialize:}\\
\quad For each arm $k \in \{1, \ldots, K\}$: initialize play counts $n_k \leftarrow 0$ and average reward estimates $Q_k \leftarrow 0$\;

\textbf{Main:}\\
\While{True}{
    $y \gets \sim \mathcal{U}(0, 1)$ \Comment*[r]{draw random variable}

    % \tcc{Exploration-Exploitation decision}
    \If{$y < \epsilon$}{
        $k^* \gets \sim \{1, \ldots, K\}$ \Comment*[r]{exploration, draw random arm}
    }
    \Else{
        $k^* \gets \arg\max_{k \in \{1, \ldots, K\}} Q_k$ \Comment*[r]{greedy exploitation}
        \tcc{Tie-breaking is typically done by picking the first entry}
    }

    \tcc{Observe the reward and update}
    Select arm $k^*$, observe reward $r_{k^*}$\;
    $n_{k^*} \gets n_{k^*} + 1$\;
    $Q_{k^*} \gets Q_{k^*} + \frac{1}{n_{k^*}} (r_{k^*} - Q_{k^*})$ \Comment*[r]{Incremental Mean Update}
}
\label{alg:epsilon_greedy}
\end{algorithm}

\noindent Since this algorithm relies on a specific choice of the exploration hyper-parameter $\epsilon$, we conducted a grid search over a sample of arm numbers $K$ for a range of $\epsilon$ values.
Figure \textbf{\ref{fig:eps_table}} shows the results of this search as an heatmap, which suggests a positive direct relationship of the two variables.
Subsequently, we modified the $\epsilon-$Greedy implementation so that $\epsilon$ is set according to the number of arms, using the function $0.4*\log_{2}(0.6*K)$, whose form and parametrization have been fit from the search data.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figure_appx_6.png}
    \caption{\textsc{Sensitivity analysis of Epsilon-Greedy} - \textit{performance of the algorithm on a piecewise stationary MAB for varying number of arms and $\epsilon$ parameter. In parenthesis is reported the decimal position of the first significant digit.}}
    \label{fig:eps_table}
\end{figure}

% ---------------------------------

\subsection{Evolutionary search} \label{list:params}
The optimization was carried out over several parameters concerning the model architecture, its dynamics, as well as three hyperparmeters of the selection process.
The following is the entire set of evolved valued, with in parenthesis the range in which they were allowed to be sampled from.
The specific choice of range has been chosen by trial and error, so that the ending distribution would not cluster over the positive or negative (if allowed) boundary, as shown in \ref{fig:evo_tabplot}.

\hfill \break
\noindent \textbf{Network parameters}
\begin{itemize}
    \item $\tau_{u}\in (1, 300)$: time constant of population $U$
    \item $\tau_{v}\in (1, 300)$: time constant of population $V$
    \item $g_{u}\in (1, 50)$: gain of the neural response function of population $U$
    \item $g_{v}\in (1, 50)$: gain of the neural response function of population $V$
    \item $s_{u}\in (0.1, 5)$: offset of the neural response function of population $U$
    \item $s_{v}\in (0.1, 5)$: offset of the neural response function of population $V$
    \item $\theta_{u}\in (-0.5, 0.9)$: threshold of the neural response function of population $U$
    \item $\theta_{v}\in (-0.5, 0.9)$: threshold of the neural response function of population $V$
    \item $W^{+}\in (2, 5)$: maximum weight value for the weights $\textbf{W}^{UV}$
\end{itemize}

\noindent \textbf{Option value function parameters}
\begin{itemize}
    \item $\beta_{v}\in (0.1, 10)$: steepness of the sigmoid
    \item $\alpha_{v}\in (-5, 5)$: shift of the sigmoid
    \item $\mu_{v}\in (-5, 5)$: mean of the Gaussian
    \item $\sigma_{v}\in (0.01, 10)$: variance of the Gaussian
    \item $r_{v}\in (-0.5, 1.5)$: weight of the sigmoid
\end{itemize}

\noindent \textbf{Learning rate function parameters}
\begin{itemize}
    \item $\beta_{\eta}\in (0.1, 10)$: steepness of the sigmoid
    \item $\alpha_{\eta}\in (-5, 5)$: shift of the sigmoid
    \item $\mu_{\eta}\in (-5, 5)$: mean of the Gaussian
    \item $\sigma_{\eta}\in (0.1, 10)$: variance of the Gaussian
    \item $r_{\eta}\in (-0.5, 1.5)$: weight of the sigmoid
\end{itemize}

\noindent \textbf{Option selection process hyperparameters}
\begin{itemize}
    \item $\text{dur}_{\text{pre}}\in (400, 3000)$: duration of the first phase
    \item $\text{dur}_{\text{post}}\in (400, 3000)$: duration of the second phase
    \item $I_{\text{ext}}\in (0.1, 1)$: magnitude of the external input
\end{itemize}

\noindent Each individual has been evaluated over two environments using the following settings:

\begin{itemize}
    \item \textsc{MAB-0}: average reward distribution entropy $\langle H\rangle=2.05$
    \item \textsc{KAB-$\sin$P}: average reward distribution entropy $\langle H\rangle=2.1$, given $K$ arm frequencies $f_{k}$ as an equally spaced set $\{0.1\ldots i\ldots 0.4\}$, phases $\lambda_{k}$ drawn from an uniform $\sim \mathcal{U}(0, 2\pi)$, and half of the arms have been set to constant values drawn from
        another uniform $\sim \mathcal{U}(0.1, 0.7)$; the final reward distribution was not normalized.
\end{itemize}

\noindent The number of arms was $K=40$ and $200$, and lasted for $2$ trials with $2000$ rounds each.
The final fit was the average of the $2$ iterations.

\hfill \break
The optimization has been implemented in Python using the \texttt{DEAP} library and the algorithm used was the \texttt{CMA-ES} algorithm. Optimization involved $40$ generations with a population size of $256$ individuals. The mutation rate was set at $0.5$ with a sigma of $0.8$, the cross-over rate was set at $0.4$.
The runs were carried out on a 256-core AMD EPYC 7763 with 2TB of RAM.

\subsubsection{Genome distribution}
Following the evolutionary search, the distribution of parameters over the top-scoring half of the population is taken, corresponding to 128 individuals.

\begin{figure}[H]
    \centering
    \includegraphics[width=1.\textwidth]{figure_appx_2.png}
    \caption{\textsc{Genome distribution} - \textit{each parameter is plotted against the fitness score.}}
    \label{fig:evo_tabplot}
\end{figure}

\noindent Figure \ref{fig:evo_tabplot} reveals the parameters shared among the models with the highest fitness score and those that are more variable.
The most stable parameters are, predicatbly, those involved in directly shaping the neural activity (neural activation functions) and learning policy (Gaussian sigmoid).

\hfill \break
For what concerns the option selection algorithm \ref{alg:decision1}, the values for the duration of the two phases were not of modest importance, with the distribution being weakly skewed towards longer times.
As shown in \ref{fig:eduration}, fitness is also apparently not associated with particular values for either phase.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.4\textwidth]{figure_appx_3.png}
    \caption{\textsc{Phase durations and fitness} - \textit{duration of the first and second phase, over the x and y axis respectively, for each individual of the top-scoring half of the population.
    The color indicates the fitness, with yellow being the highest.}}
    \label{fig:eduration}
\end{figure}

\noindent The effect of different durations for the first and second phases on performance is limited, as shown in Figure \ref{fig:eduration}.


\hfill \break
\subsection{Multiple runs on multiple environments}\label{sec:appendix_reps}
\noindent The results reported in the table of results \ref{tab:results} were obtained by running all models on all environments on a variable number of arms for multiple times (64).
Figure \textbf{\ref{fig:heat_res}} illustrates the outcome of the runs for each simulation setting.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figure_appx_7}
    \caption{\textsc{Heatmap of multiple runs} - \textit{Each single heatmap shows the average regret (white=low, dark red=high) of two trials for a single model on a given environment for a specific number of arms $K$.}}
    \label{fig:heat_res}
\end{figure}

\noindent Notably, the NSA model performs worst in the piecewise stationary with drift scenarion (MAB-D), while outcompeting the others in the sinusoidal cases and when $K$ is large.

\hfill \break
\subsection{Reward distribution entropy}\label{sec:appendix_entropy}

\noindent The calculation of a set of $N$ reward probability distribution $\mathbf{p}_{i}\text{  for  } i\ldots N$ for $K$ values with a progressive level of entropy $\mathbf{h}_{i}\text{  for  } i\ldots N$ has been obtained by modifying a starting reference distribution $p_{\text{ref}}$.
A description of the algorithm is presented below \ref{alg:entr_alg}.
\hfill \break

\begin{algorithm}[H]
\caption{Reward probability distribution generation}
\SetAlgoLined
\KwIn{Number of distributions $N$, dimension $K$}

\KwOut{Set of probability distributions ${\mathbf{p}_i}$ with decreasing entropy}
\SetKwComment{Comment}{// }{ }
\textbf{Define:}\\
%Define set $B = \{17, 15, 12, 8, 4, 1.5, 0.5\}$; \\
%Define set $B = \{2^{-i}\}$; \\
\qquad$p_{\text{ref}}\gets\;\sim \mathcal{N}(0.2, 0.13)^K$ \Comment*[r]{reference distribution}
\qquad$p_{\text{ref}}\gets\; \text{clip}(p_{\text{ref}}, 0, 1)$ \Comment*[r]{clipping}
\qquad$\Lambda\gets\;\{\lambda_{i}: 1.3^{-i}\;\vert\;i\in\text{linspace}(0, 6, N)\}$ \Comment*[r]{softmax temperature}

\textbf{Main:}\\
\For{$i \gets 0$ to $N$}{
    %$\mathbf{z} \gets \;\sim \mathcal{U}(0,p_{\text{max}})^K$ \Comment*[r]{sample random vector}
    $\mathbf{z} \gets p_{\text{ref}}$ \Comment*[r]{copy reference distribution}
    $\mathbf{z} \gets \text{exp}(\lambda_{i} * \mathbf{z}) * \left(\sum_{j} \text{exp}(\lambda_{i} * z_{j})\right)^{-1}$ \Comment*[r]{softmax}
    $\mathbf{z} \gets \mathbf{z} * \sum_{j} z_{j}$ \Comment*[r]{rescaling}
    $\mathbf{z} \gets \text{clip}(\mathbf{z}, 0, 1)$ \Comment*[r]{clipping}

    %$\mathbf{z}_j \gets 1$;\\
    %$\beta_i \gets \text{Sample index=} i \text{ from }(B)$ \Comment*[r]{Sample temperature from $B$}
    %$\mathbf{p}_i \gets \frac{\exp(\beta_i \mathbf{z})}{\sum_j \exp(\beta_i \mathbf{z}_j)}$ \Comment*[r]{Softmax with temperature}
}
\Return ${\mathbf{p}_i}$
\end{algorithm}\label{alg:entr_alg}

\hfill \break
\noindent Given the reference $p_{\text{ref}}$ and its entropy $H_{\text{ref}}$, it is possible to quantify the difference in entropy with respect to each derived distribution $i$ as $\Delta H=H_{\text{ref}} - H_{i}$.
This measure is informative about how much the best arm stands out from the others.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{figure_appx_5}
    \caption{\textsc{Showcase of distribution entropy} - \textbf{a}: \textit{visualization of each distribution as heatmaps (columns), the entropy differences (x-axis) and arm indexes (y-axis); note that the entropy of distribution $i$ is inversely proportional to the difference $\Delta H$} -
        \textbf{b}: \textit{log-log plot of the entropy differences (x-axis) against the scaling $\lambda$ of the top-arm with respect to the others (y-axis)}.}
    \label{fig:entropy_sw}
\end{figure}

In Figure \ref{fig:entropy_sw} are illustrated examples of distributions with decreasing levels of entropy difference with the reference by means of a heat map in \ref{fig:entropy_sw}\textbf{a}.
Both plots highlight the blending of the probability associated with the best arm with the other ones, eventually becoming indistinguishable from the rest.
In the MAB taks, this increase in entropy translates in a higher difficulty in picking the best option, although the other side of the medal is that regret also decreases, since the cost of selecting a non-optimal arm is less impactful on performance.
Plot \ref{fig:entropy_sw}\textbf{b} shows the relationship between the scaling of the difference between the best arm and the rest.

% ---------------------------------

% weight update over rounds
\subsection{Weight update dynamics}
\noindent We also analyzed the weight update dynamics of the model over the rounds.
In figure \ref{fig:rew_update}, we plotted the evolution of the total weight $\Delta W^{UV}$ over time, averaged over 20 simulations, and smoothed over 30 rounds.
The results show that the model can quickly adapt to new reward distributions. It is also able to maintain the optimal policy over time, with the weights remaining approximately stable.
The quantity of updates $\Delta W_{k}^{UV}$, which in each round is applied to one connection $k$, changes sign according to the collected reward, with its magnitude higher at the beginning of the trials.
Initially, the sign is mostly positive (potentiation) since the weights start at zero, and after some uncertainty a consistently preferred arm emerges.
However, when the reward distribution switches, a regular series of suboptimal choices is made with respect to the new distribution, leading to zero reward.
This causes an accumulation of negative sign weight updates (depression), eventually causing the value of the preferred arm to drop. In the meantime, other options are probed until another sequence of choices converges to another arm, promoted by a trail of positive weight updates.

This behavior is consistent with the low entropy levels observed in the previous analysis.


\begin{figure}[H]
    \centering
    \includegraphics[width=1.0\textwidth]{figure_appx_4.png}
    \caption{\textsc{Weight update developement for the model} \textit{The plot displays the weight update quantity $\Delta W_{k}^{UV}$ for each round (blue line), smoothed as a 20-steps moving average.
    It is also reported the average reward in a window of 30 rounds (orange line). The results have been obtained averaging over 30 iterations.}}
    \label{fig:rew_update}
\end{figure}






